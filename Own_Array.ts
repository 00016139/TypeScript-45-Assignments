let Transports : string[] = ["Bike", "Car", "Plane"]; // declare a variable of transport
Transports.forEach(Transport => {
    console.log(`I want to buy ${Transport}`);
}) // use foreach function to print seprately